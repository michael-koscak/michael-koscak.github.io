source "https://rubygems.org"

# Use the GitHub Pages gem to match the build environment
gem "github-pages", group: :jekyll_plugins

# Lock to Ruby 3 where possible (GitHub Pages uses a managed Ruby version)
# ruby "3.1.0"

